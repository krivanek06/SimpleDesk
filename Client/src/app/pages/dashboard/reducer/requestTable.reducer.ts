import * as RequestAction from '../action/requestTable.action';
import {RequestTable} from "../../../core/model/Request";

/*
export type Action = RequestAction.All;



export function requestReducer(state: RequestTable, action: Action) {
  console.log(action.type, state);
  switch (action.type) {
    case RequestAction.ASSIGN_REQUEST:
      return {...state, }
  }
}*/
